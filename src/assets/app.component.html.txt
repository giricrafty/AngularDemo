<div class="row" style="background-color: rgb(56, 53, 252); color:white">
    <h1 style="font-size: 200%; padding-top: 20px; padding-bottom: 20px; font-weight: bold ;"
        class="col-10 d-flex justify-content-center">Demo Tool</h1>
    <button class="col" mat-raised-button color="primary" type="button" (click)="generateForm()">Generate Form</button>
</div>

<div class="p-3">
    <div class="my-2">
        <form>
            <mat-form-field style="width:17%;margin-left: 1%;" appearance="fill">
                <mat-label>Component</mat-label>
                <mat-select name="component" (selectionChange)="onChange()" [(ngModel)]="selectedComponent">
                    <mat-option *ngFor="let fields of fieldsList" [value]="fields">{{fields}}</mat-option>
                </mat-select>
            </mat-form-field>
            <div style="width:40%;margin-left: 1%;margin-top: 100px;" [hidden]="hideUserInput">
                <label>Enter the Number of columns</label>
                <input type="text" [(ngModel)]="NoOfColumns" #box1>
                <br>
                <label>Enter the Number of Rows</label>
                <input type="text" [(ngModel)]="NoOfRows" #box2>
                <button class="btn btn-primary" (click)="askControllers(box2.value,box1.value)">Select the controllers for Rows {{box2.value}} & columns {{box1.value}}</button>
                <!-- <label>Select the controllers for {{box2.value}} Rows {{box1.value}} cols</label> -->
            </div>
             
            <div style="width:40%;margin-left: 1;" *ngFor="let controll of total">
            
                <mat-form-field style="width:20%;margin-left: 1%;" appearance="fill" >
                    <mat-label>Component</mat-label>
                    <mat-select name="component" [(ngModel)]="selectedComponent">
                        <mat-option *ngFor="let fields of fieldsList" [value]="fields">{{fields}}</mat-option>
                    </mat-select>
                </mat-form-field>
                <mat-form-field style="width:30%;margin-left: 1%;" appearance="fill">
                    <mat-label>Events</mat-label>
                    <mat-select name="events" multiple [(ngModel)]="selectedEvents">
                        <mat-option *ngFor="let events of eventList" [value]="events">{{events}}</mat-option>
                    </mat-select>
                </mat-form-field>
                <mat-form-field style="width:30%;margin-left: 1%;" appearance="fill">
                    <mat-label>Validation</mat-label>
                    <mat-select name="validation" multiple [(ngModel)]="selectedValidation">
                        <mat-option *ngFor="let valid of validationList" [value]="valid">{{valid}}</mat-option>
                    </mat-select>
                </mat-form-field>
            </div>
        
            <mat-form-field style="width:40%;margin-left: 1%;" appearance="fill" [hidden]="hideEvent">
                <mat-label>Events</mat-label>
                <mat-select name="events" multiple [(ngModel)]="selectedEvents">
                    <mat-option *ngFor="let events of eventList" [value]="events">{{events}}</mat-option>
                </mat-select>
            </mat-form-field>

            <mat-form-field style="width:40%;margin-left: 1%;" appearance="fill" [hidden]="hideValidation">
                <mat-label>Validation</mat-label>
                <mat-select name="validation" multiple [(ngModel)]="selectedValidation">
                    <mat-option *ngFor="let valid of validationList" [value]="valid">{{valid}}</mat-option>
                </mat-select>
            </mat-form-field>
            <!-- <input type="radio" id="materialui" #radio name="style_language" value="materialui" [(ngModel)]="selectedStyle" (change)="radioselect(radio.value)">
            <label for="materialui" >Material UI</label><br>
            <input type="radio" id="bootstrap"  #radio name="style_language" value="bootstrap">
            <label for="Bootstrap">Bootstrap</label><br>  -->

            <button mat-raised-button color="primary" style="float:right;" type="button"
                (click)="btnClick()">Submit</button>
            <button mat-raised-button color="primary" style="float:right;" type="button"
                (click)="btnNavigate()">Test</button>
        </form>
        <div *ngIf=testcases>
            <app-test></app-test>
        </div>
    </div>
    <br />
    <div class="my-4">
        <mat-tab-group>
            <mat-tab label="HTML">
                <pre>
                            <code>
                                {{htmlsnippet}}
                            </code>
                        </pre>
            </mat-tab>
            <mat-tab label="TS">
                <pre>
                            <code [innerHTML]="tssnippet">
                            </code>
                        </pre>
            </mat-tab>
            <mat-tab label="SPEC TS">
                <pre>
                            <code [innerHTML]="specsnippet">
                            </code>
                        </pre>
            </mat-tab>
            <mat-tab label="BOOTSTRAP">
                <pre>
                            <code>
                                {{bootstraphtmlsnippet}}
                            </code>
                        </pre>
            </mat-tab>
        </mat-tab-group>
    </div>
</div>


<router-outlet></router-outlet>